general {
    hide_cursor = true
}
background {
    monitor =
    color = rgb(171e2b)
}
input-field {
    monitor =
    size = 300, 50
    position = 0, -40
    halign = center
    valign = center
    outer_color = rgb(6482a6)
    inner_color = rgb(232f40)
    font_color = rgb(dce5ef)
    font_family = Noto Sans CJK JP
    placeholder_text = Password
    fade_on_empty = false
}
label {
    monitor =
    text = $TIME
    font_size = 56
    color = rgb(dce5ef)
    position = 0, 80
    halign = center
    valign = center
}
