[package]
name = "wheelpad"
version = "0.2.0"
edition = "2021"
description = "Circular scrolling for Let's Note touchpads on Wayland"

[dependencies]
evdev = "0.13.2"
clap = { version = "4.5", features = ["derive"] }

[profile.release]
lto = true
strip = true
